# Spring Cloud Function Configuration
# Specify which function to use (can be overridden by AWS Lambda environment variables)
spring.cloud.function.definition=uppercase

# Application name
spring.application.name=aws-lambda-function

# Logging configuration
logging.level.root=INFO
logging.level.com.example.lambda=DEBUG
